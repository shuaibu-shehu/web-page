import ProjectEditor from "@/components/admin/project-editor";

export const metadata = { title: "New Project — CodeTherapy Admin" };

export default function NewProjectPage() {
  return <ProjectEditor project={null} />;
}
