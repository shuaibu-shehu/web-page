import PartnerEditor from "@/components/admin/partner-editor";

export const metadata = { title: "Add Partner — CodeTherapy Admin" };

export default function NewPartnerPage() {
  return <PartnerEditor partner={null} />;
}
